version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      

  build:
    commands:
      - echo Build started
      - python app.py || echo "No app file"
      - python test_math.py || echo "No app file"

  post_build:
    commands:
      - echo Build completed

artifacts:
  files:
    - '*/'
